kong:
  adminUrl: "http://kong-admin.k8s.shub.us"
  adminToken: ""  # 如果启用了 RBAC

controller:
  watchNamespace: "cert-manager"
  labelSelector: ""  # 为空表示同步所有证书
  resyncPeriod: "30m"
  clusterName: "eks-test"

logging:
  level: "info"
  format: "json"