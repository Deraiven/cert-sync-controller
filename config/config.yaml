kong:
  adminUrl: "http://kong-kong-admin.default:8001"
  adminToken: ""  # 如果启用了 RBAC

controller:
  watchNamespace: "cert-manager"
  labelSelector: ""  # 为空表示同步所有证书
  resyncPeriod: "30m"
  clusterName: "storehub-uat"

logging:
  level: "info"
  format: "json"
